// SceneMount is intentionally empty — the camera canvas is embedded
// directly inside the CameraSection in Portfolio.tsx using position:absolute,
// so it only appears within that section's bounds, not fixed over the page.
export function SceneMount() {
  return null;
}
